<?php

$this->headTitle('Search Results'); 
$this->layout()->bodyClass = 'search-results-page';
?>

<div class="search-form-container">
    <h2>YOU SEARCHED FOR:</h2>
    
    <form method="GET" action="<?php echo $this->url('site/united-search', [], true); ?>" class="search-form">
        <div class="search-fields">
            <div class="field">
                <label for="state">State <?php if(isset($state) && $state): ?><span class="required">*</span><?php endif; ?></label>
                <select name="state" id="state" class="form-control">
                    <?php if(isset($state) && $state): ?>
                        <option value="<?php echo $this->escapeHtml($state); ?>"><?php echo $this->escapeHtml($state); ?></option>
                    <?php endif; ?>
                    <?php foreach ($states as $stateOption): ?>
                        <?php if (!isset($state) || $stateOption != $state): ?>
                            <option value="<?php echo $this->escapeHtml($stateOption); ?>"><?php echo $this->escapeHtml($stateOption); ?></option>
                        <?php endif; ?>
                    <?php endforeach; ?>
                </select>
            </div>
            
            <div class="field">
                <label for="county">County (optional)</label>
                <select name="county" id="county" class="form-control">
                    <?php if(isset($county) && $county): ?>
                        <option value="<?php echo $this->escapeHtml($county); ?>"><?php echo $this->escapeHtml($county); ?></option>
                    <?php endif; ?>
                    <option value="">Select County</option>
                    <?php if(isset($counties)): ?>
                        <?php foreach ($counties as $countyOption): ?>
                            <?php if (!isset($county) || $countyOption != $county): ?>
                                <option value="<?php echo $this->escapeHtml($countyOption); ?>"><?php echo $this->escapeHtml($countyOption); ?></option>
                            <?php endif; ?>
                        <?php endforeach; ?>
                    <?php endif; ?>
                </select>
            </div>
            
            <div class="search-button">
                <button type="submit" class="button search-submit">Search</button>
            </div>
        </div>
    </form>
</div>

<!-- Original search results content below -->
<?php echo $this->content; ?>